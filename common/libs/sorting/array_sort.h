

////
////	File:	array_sort.h
////	Author:	Ritchie Brannan
////	Date:	11 Nov 10
////
////	Description:
////
////		AVL array sorting class.
////

////
////	include guard begin
////


#pragma once

#ifndef	__ARRAY_SORT_INCLUDED__
#define	__ARRAY_SORT_INCLUDED__


////
////	includes
////


#include "libs/system/types.h"


////
////	begin sorting namespace
////


namespace sorting
{


////
////	Array query interface
////
////    notes:
////
////        get()   :   returns a key.
////        cmp()   :   compares a key against the the indexed item.
////        move()  :   moves an item from one indexed location to another.
////
////        key will never be NULL.
////
////        index will never be negative.
////
////        source and target will never be the same value.
////
////        source or target but not both may have a value of -1 indicating that the location is temporary storage.
////
////        Pointers returned by the get() function are not cached, only the most recently returned pointer is required.
////


INTERFACE_BEGIN( IArrayQuery )
    virtual const void*     get( const int index ) const = 0;
    virtual int             cmp( const void* const key, const int index ) const = 0;
    virtual void            move( const int source, const int target ) = 0;
INTERFACE_END();


////
////	ArraySort class
////


class ArraySort
{
public:
	inline                  ArraySort() : m_rootIndex( -1 ), m_freeIndex( -1 ), m_nodeCount( 0 ), m_usedCount( 0 ), m_peakCount( 0 ), m_nodes( NULL ), m_query( NULL ) {};
	inline                  ArraySort( IArrayQuery& query, const unsigned int count = 0 ) : m_rootIndex( -1 ), m_freeIndex( -1 ), m_nodeCount( 0 ), m_usedCount( 0 ), m_peakCount( 0 ), m_nodes( NULL ), m_query( NULL ) { init( query, count ); };
	virtual                 ~ArraySort() { kill(); };
    bool                    init( IArrayQuery& query, const unsigned int count = 0 );
    void                    kill();
    void                    reset();
    inline bool             valid() const { return( ( ( m_nodes != NULL ) && ( m_query != NULL ) ) ? true : false ); };
    inline bool             contains( const int index ) const { return( ( valid() && ( static_cast< unsigned int >( index ) < static_cast< unsigned int >( m_nodeCount ) ) && ( m_nodes[ index ].flag == k_used ) ) ? true : false ); };
	int                     insert( const void* const key );
	bool                    update( const int index );
	bool                    remove( const int index );
	bool                    relink();
	bool                    sort();
    int*                    rank() const;
    int                     rank( const int index ) const;
	int                     find( const void* const key ) const;
	int                     prev( const int index ) const;
	int                     next( const int index ) const;
	int                     root() const;
	int                     first() const;
	int                     last() const;
    inline unsigned int     used() const { return( static_cast< unsigned int >( m_usedCount ) ); };
    inline unsigned int     size() const { return( static_cast< unsigned int >( m_peakCount ) ); };
	inline int              height() const { return( ( m_nodes != NULL ) ? height( m_rootIndex ) : 0 ); };
	inline int              weight() const { return( ( m_nodes != NULL ) ? weight( m_rootIndex ) : 0 ); };
	inline bool             validate() const { return( ( ( m_nodes != NULL ) && ( m_query != NULL ) ) ? validate( m_rootIndex ) : false ); };
protected:
    struct Node
    {
	    int                 parent;
	    int                 children[ 2 ];
	    short               balance;
	    short               flag;
    };
    int                     allocate();
    void                    recycle( const int index );
    int                     linear();
    int                     sorted( const int parent, const int lower, const int upper );
    inline int              sorted() { return( sorted( -1, 0, ( m_usedCount - 1 ) ) ); };
    void                    extend( const int lower, const int upper );
    inline void             extend() { extend( m_usedCount, ( m_nodeCount - 1 ) ); };
    bool                    insert( const int index, const void* const key );
    bool                    unlink( const int index );
    void				    rotate( const int index );
	int					    height( const int index ) const;
	int					    weight( const int index ) const;
	bool				    validate( const int index ) const;
    int                     m_rootIndex;    //  index of the root node
    int                     m_freeIndex;    //  index of the first free node
    int                     m_nodeCount;    //  count of nodes allocated
    int                     m_usedCount;    //  count of nodes occupied
    int                     m_peakCount;    //  peak count of nodes occupied
	Node*                   m_nodes;        //  array of allocated nodes
    IArrayQuery*            m_query;        //  indexable data query object
    static const short      k_used = 0x1248;
    static const short      k_free = 0x4182;
};


////
////	end sorting namespace
////


};	//	namespace sorting


////
////	include guard end
////


#endif	//	#ifndef	__ARRAY_SORT_INCLUDED__


////
////	end of file
////


